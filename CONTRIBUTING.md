# Branches

* `main` is always clean. Everything must work as intended, and all tests must pass.
* `dev-godot` is allowed to not have godot working. The rust tests must pass.
* `dev-rust` is allowed to break everything.

All branches must merge into a matching development branch.